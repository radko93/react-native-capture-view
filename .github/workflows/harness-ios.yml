name: Harness iOS

on:
  push:
    branches:
      - main
    paths:
      - 'ios/**'
      - 'src/**'
      - 'example/**'
      - '*.podspec'
      - 'package.json'
      - '.github/workflows/harness-ios.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'ios/**'
      - 'src/**'
      - 'example/**'
      - '*.podspec'
      - 'package.json'
      - '.github/workflows/harness-ios.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  harness-ios:
    runs-on: macos-26
    timeout-minutes: 20

    env:
      RCT_USE_RN_DEP: 1
      RCT_USE_PREBUILT_RNCORE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup
        uses: ./.github/actions/setup

      - name: Install cocoapods
        run: |
          cd example
          bundle install
          bundle exec pod install --project-directory=ios || \
            (rm -rf ios/Pods ios/Podfile.lock && bundle exec pod install --project-directory=ios)

      - name: Build iOS app (Debug)
        run: |
          cd example/ios
          xcodebuild \
            -workspace CaptureViewExample.xcworkspace \
            -scheme CaptureViewExample \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build \
            | xcpretty --color && exit ${PIPESTATUS[0]}

      - name: Run harness tests
        uses: callstackincubator/react-native-harness@0da1aef3c45964f14c00513b27f836cd9ae0d74f # v1.1.0
        with:
          app: ios/build/Build/Products/Debug-iphonesimulator/CaptureViewExample.app
          runner: ios
          projectRoot: example
